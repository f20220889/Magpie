# Runtime deps — all free / open source. Pin loosely; lock later.
typer>=0.12
rich>=13.7
httpx>=0.27
trafilatura>=1.9
ddgs>=6.0
ollama>=0.3
sentence-transformers>=3.0
pydantic>=2.7
pydantic-settings>=2.3
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
# Postgres driver — only needed when DATABASE_URL points at Postgres (deploy).
psycopg[binary]>=3.1

# Dev
pytest>=8.0
ruff>=0.5
